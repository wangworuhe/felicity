.calendar {
  background: #FFFFFF;
  padding: 8px 0 4px;
}

/* 顶部导航 */
.calendar-header {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 6px;
  position: relative;
}

.month-title-wrap {
  display: flex;
  align-items: center;
  gap: 4px;
}

.month-title {
  font-size: 15px;
  font-weight: 600;
  color: #1F3B6D;
  min-width: 100px;
  text-align: center;
}

.month-title-arrow {
  font-size: 10px;
  color: #999;
}

.today-btn {
  position: absolute;
  right: 4px;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  border: 1px solid #FF6B6B;
}

.today-btn:active {
  background: #FFF0F0;
}

.today-btn-text {
  font-size: 12px;
  color: #FF6B6B;
  font-weight: 600;
}

/* 星期头 */
.weekday-row {
  display: flex;
  margin-bottom: 2px;
}

.weekday-cell {
  flex: 1;
  text-align: center;
  padding: 2px 0;
}

.weekday-text {
  font-size: 11px;
  color: #bbb;
}

/* 滚动容器 */
.weeks-scroll {
  overflow: hidden;
}

/* 月份分隔标记 */
.month-separator {
  padding: 6px 12px 2px;
}

.month-separator-text {
  font-size: 12px;
  color: #bbb;
  font-weight: 500;
}

/* 周行 */
.week-row {
  display: flex;
}

/* 日期格子 */
.day-cell {
  width: 14.2857%;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 1px 0;
}

.day-inner {
  width: 34px;
  height: 34px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 8px;
  position: relative;
}

.day-inner:active {
  background: #f5f5f5;
}

.day-text {
  font-size: 14px;
  color: #333;
  line-height: 1;
}

/* 非当月日期 */
.day-inner.other-month .day-text {
  color: #ddd;
}

/* 今天 */
.day-inner.today .day-text {
  font-weight: 700;
}

/* 选中 */
.day-inner.selected {
  background: #FF6B6B;
}

.day-inner.selected .day-text {
  color: #FFFFFF;
  font-weight: 600;
}

.day-inner.selected .dot {
  background: #FFFFFF;
}

/* 未来日期（不可点击） */
.day-inner.future .day-text {
  color: #e0e0e0;
}

.day-inner.future:active {
  background: transparent;
}

/* 有记录的红点 */
.dot {
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: #FF6B6B;
  margin-top: 1px;
}

/* 年月选择器 */
.picker-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.3);
  z-index: 100;
}

.picker-panel {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #fff;
  border-radius: 16px 16px 0 0;
  z-index: 101;
  padding-bottom: env(safe-area-inset-bottom);
}

.picker-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 20px;
  border-bottom: 1px solid #f0f0f0;
}

.picker-cancel {
  font-size: 15px;
  color: #999;
}

.picker-title {
  font-size: 16px;
  font-weight: 600;
  color: #333;
}

.picker-confirm {
  font-size: 15px;
  color: #FF6B6B;
  font-weight: 600;
}

.picker-view {
  width: 100%;
  height: 200px;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  color: #333;
}
